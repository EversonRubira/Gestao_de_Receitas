<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/backoffice.css">
</head>
<body class="backoffice">
    <%- include('../../partials/backoffice-header') %>

    <div class="backoffice-container">
        <%- include('../../partials/backoffice-sidebar') %>

        <main class="backoffice-content">
            <div class="page-header">
                <h1>Gestão de Receitas</h1>
                <a href="/backoffice/receitas/nova" class="btn btn-primary">➕ Nova Receita</a>
            </div>

            <div class="table-container">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nome</th>
                            <th>Categoria</th>
                            <th>Dificuldade</th>
                            <th>Tempo</th>
                            <th>Custo</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (receitas.length > 0) { %>
                            <% for (let i = 0; i < receitas.length; i++) { %>
                                <tr>
                                    <td><%= receitas[i].id %></td>
                                    <td><%= receitas[i].nome %></td>
                                    <td><%= receitas[i].categoria_nome %></td>
                                    <td>
                                        <span class="badge">
                                            <%= receitas[i].dificuldade_nivel %>
                                        </span>
                                    </td>
                                    <td><%= receitas[i].tempo_preparacao %> min</td>
                                    <td>€<%= receitas[i].custo %></td>
                                    <td class="actions">
                                        <a href="/receita/<%= receitas[i].id %>" class="btn btn-small btn-info">Ver</a>
                                        <a href="/backoffice/receitas/editar/<%= receitas[i].id %>" class="btn btn-small btn-warning">Editar</a>
                                        <form method="POST" action="/backoffice/receitas/eliminar/<%= receitas[i].id %>" style="display:inline;">
                                            <button type="submit" class="btn btn-small btn-danger">Eliminar</button>
                                        </form>
                                    </td>
                                </tr>
                            <% } %>
                        <% } else { %>
                            <tr>
                                <td colspan="7" class="text-center">Nenhuma receita encontrada.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>
